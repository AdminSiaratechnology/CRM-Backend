[project]
name = "crm-fastapi-backend"
version = "0.1.0"
description = "FastAPI backend for Nexus CRM"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["app/tests"]

